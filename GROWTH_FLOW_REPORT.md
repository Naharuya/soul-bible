# 저장 → 작은 성장 기록 → 다음날 알림

2026-09-11. AI API를 변경하거나 실제 호출하지 않고 앱 흐름을 보강했다.

- 홈에 `매일 3분 마음대화`를 표시한다. 대화를 시간 제한으로 종료하지 않는다.
- 기존 말씀·작은 행동·십자가 게임을 거쳐 마음카드에서 저장과 공유를 선택한다. 저장에 성공하면 `작은 성장 기록`으로 이동한다. 공유는 기존 미리보기와 시스템 공유창 경로를 유지한다.
- 성장 기록은 별도 누적 점수 없이 저장된 카드의 날짜·선택한 행동·말씀 출처를 보여준다. 행동을 실제 수행했다고 판정하지 않는다. 저장된 카드를 삭제하면 해당 성장 기록도 제거된다. 메뉴에서도 다시 열 수 있다.
- 기록 화면에서 다음날 알림 설정으로 이동한다. 직접 알림을 켜고 기기 권한이 허용된 경우에만 예약한다. 시작 날짜를 설정과 함께 보존하므로 당일 재시작·재방문에도 알림이 오늘로 앞당겨지지 않는다. 다음날 이후에는 선택한 시간에 매일 예약한다. 기존 위기 상황 알림 중지 동작을 유지한다.
- 기록을 불러오지 못하면 재시도할 수 있다. 카드 관리에서 돌아오면 기록을 다시 읽는다.

검증: Flutter 전체 70개 통과·기존 skip 1개. 정상/모의 실패 대화 후 저장·성장 화면 전환, 기록에서 알림 진입 시 자동 동의 없음, 명시적 활성화 시 다음날 예약, 연도 경계·자정·재시작·다음 회차 예약 검증. 정적 분석 error/warning 0·기존 info 6개.

릴리스 APK 빌드 및 SM-S908N USB 업데이트 `Success`, 앱 실행 요청 `Status: ok`. 실제 단말의 전체 사용 흐름과 다음날 알림 수신은 아직 확인하지 않았다.

코드: [성장 기록](lib/features/growth_page.dart), [저장 후 이동](lib/features/conversation_page.dart), [알림 설정](lib/engagement/notifications/notification_settings_page.dart), [예약 로직](lib/engagement/notifications/reminder_controller.dart).
