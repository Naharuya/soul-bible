# onaria 완료 판정 기준

작성일: 2026-09-10. 문서 연결: [현재 상태](ONARIA_STATUS.md) · [진행 순서](ONARIA_ROADMAP.md) · **완료 판정 기준**.

사용자가 정한 네 단계를 근거로 판정한다. 아래 항목별 기준은 실행 가능한 검증 기준의 초안이며, 미정인 제품 범위·사업 목표·담당자를 대신 확정하지 않는다.

## 공통 단계

| 단계 | 완료 조건 | 완료로 인정하지 않는 것 |
| --- | --- | --- |
| ① CODE | 정해진 범위가 실행 경로에 연결되고 설정·저장·실패 처리가 구현됨. 변경 파일/커밋과 범위 기록 | 클래스·화면·프롬프트 파일만 존재, 연결되지 않은 메뉴, TODO·mock만 존재 |
| ② TEST | 해당 범위의 적절한 자동 테스트 통과. 명령·결과·대상 버전·환경·mock·skip 기록 | 관련 없는 전체 테스트 개수, 설명 문서, 빌드 성공만으로 기능 검증 대체 |
| ③ REAL | 목표 서버·단말·플랫폼에서 실제 의존성을 사용해 지정 흐름·실패·복구를 확인. 버전·설정·날짜·결과 기록 | mock 응답, 로컬 성공을 운영 성공으로 전환, 설치·실행을 전체 기능 확인으로 전환 |
| ④ USER | 명시한 대상 사용자가 지정 과제를 수행하고 수용 기준별 결과·피드백이 확인됨 | 개발자 추정, 합성 페르소나, 테스트 통과, 실행 화면만 확인 |

상태값은 **완료 / 부분 완료 / 미완료 / 미확인 / 해당 없음**이다. STATUS의 ‘핵심 구현’, ‘mock 통과’ 등 표현은 검증 범위를 제한한 설명이며 항목 전체 완료를 뜻하지 않는다. ‘미완료’는 남은 조건이 확인된 경우, ‘미확인’은 판단 증거가 없는 경우다. ‘해당 없음’은 이유를 적고 단계 실패를 감추는 데 사용하지 않는다.

사업 실험에서 CODE·TEST가 직접 해당하지 않을 수 있다. 측정 시스템의 CODE·TEST는 Analytics·D7·결제 등 관련 항목에서 별도로 판정한다. 라이선스는 검토 도구의 CODE·TEST와 실제 사용권 근거·검토 결과를 분리한다.

모든 적용 가능한 단계와 항목별 필수 조건이 충족돼야 해당 범위를 전체 완료로 보고한다. 새 코드·환경·모델·자료 변경이 이전 증거를 무효화하면 영향받는 단계만 다시 검증한다. 불필요하게 모든 검증을 반복하지 않는다.

## 항목별 필수 증거

공통 CODE·TEST 조건에 더해 다음 내용을 확인한다. 사용자 검증 대상과 규모는 제품 기능·전문 검수·운영자 작업에 맞게 정하고, 임의의 사용자 수나 점수 기준을 만들지 않는다.

| 항목 | 핵심 검증과 REAL 완료 조건 | USER 또는 외부 수용 근거 |
| --- | --- | --- |
| Safety / 위기대응 | 합성 위기 입력에서 고정 지원 경로, 일반 모델/검색 우회, 연결 장애 시 도움 UI. 테스트 중 실제 긴급전화는 발신하지 않음 | 전문가 검토와 지원 문구·접근성 검증. 일반 사용자에게 실제 위기 노출을 요구하지 않음 |
| OpenAI 실제 연결 | 실제 제공자 요청 식별자·모델·usage 확인, fallback과 구분, 단말 출력까지 연결 | 실제 대화 과제 수행과 응답 품질 피드백 |
| Psychology Agent | 실제 모델의 계약 준수, 금지된 단정·과도한 조언 방지, 실패 시 처리 | 검토자와 대상 사용자의 공감·명료성·적절성 평가 |
| Bible RAG | 승인 자료·버전·활성 인덱스·검색 근거·인용 연결·검색 실패 검증 | 대상 사용자의 출처 이해·내용 적절성, 관련 전문가 검수 |
| 성경 라이선스 | 번역본·언어·매체·지역·상업적 이용 등 사용 범위와 근거, 검토 결과, 배포 자료 버전 연결 | 담당 검토자의 확인. 사용자 만족도나 metadata 문자열로 사용권을 대체하지 않음 |
| Response Integrator | 실제 전문 응답 통합, 계약·중복·충돌·누락·인용 오류 처리, 단말 렌더링 | 사용자가 통합 응답과 다음 행동을 이해하는지 확인 |
| 7화면 MVP | 확정된 7화면 각각의 진입/뒤로가기/종료, 정상·오류 흐름, 단말 전체 과제 | 사용자 핵심 과제 완료와 혼란·막힘 기록 |
| Persistent Memory | 서버/앱 재시작 후 복원, 사용자 간 격리, 삭제 후 재등장 방지, 저장 실패 처리 | 기억 내용·수정/삭제 경험이 사용자 기대에 맞는지 확인 |
| 익명 Analytics | 실제 이벤트부터 집계까지 추적, 중복·누락·재전송 처리, 정한 데이터 최소화·선택 규칙 준수 | 설명과 선택권 검증, 운영자의 보고서 해석 확인. 가명 코호트 추적을 무조건 익명이라고 부르지 않음 |
| 재방문 알림 | 실제 권한 허용/거절·수신·취소·앱 복귀, 대상 OS의 재시작/시간대 동작 | 사용자가 설정하고 끌 수 있으며 빈도·문구를 수용하는지 확인 |
| 십자가 미니게임 | 메뉴 접근, 완료/중단/재진입, 상태·이벤트 확인 | 플레이 이해도·부담·재사용 피드백 |
| 마음카드/말씀 공유 | 실제 렌더·공유시트·외부 수신 결과·취소·파일 저장 검증 | 공유 결과 가독성과 민감 내용 노출에 대한 사용자 선택 확인 |
| 이스터에그/가벼운 게임화 | 실제 조건 충족·중복 방지·복원·해제 표시 | 보상 의미·방해 여부·압박감에 대한 피드백 |
| Closed Beta 50명 | 정의된 참여 조건을 충족한 실제 50명 규모와 배포·관찰 기록. 초대 수와 실참여 수 구분 | 사전 정의한 과제·피드백 수집 결과. 개별 기능의 USER 완료를 일괄 추정하지 않음 |
| D7 Retention | 정의된 코호트의 7일 관찰 완료, 정확한 분자/분모·제외 기준·재현 가능한 집계 | 실제 사용자의 재방문 실적. 측정 완료와 목표 달성 별도 판정 |
| 100명 교회 PoC | 합의한 실제 100명 규모·역할·기간·운영 조건과 사용 기록 | 교회 담당자·참여자 평가, 합의한 성공 기준별 결과 |
| Premium 검증 | 실제 결제와 권한 부여·취소·갱신의 일치, 장애·중복 이벤트 처리 | 실제 유료 전환·사용 가치·해지 이유. 설문 의향을 결제 실적으로 대체하지 않음 |
| AI Cost Controller | 운영 usage·원장·제공자 비용 대조, 예산 차단·재시작·동시성·fallback, 비용 범위 명시 | 운영자가 비용을 설명·통제할 수 있고 사용자가 제한 경험을 이해하는지 확인 |
| CAC/LTV | 출처가 있는 획득비·전환·매출·원가·유지기간, 산식 재현 | 실제 코호트 행동·결제로 가정 검토. 짧은 관찰기간의 추정 LTV를 확정값으로 보고하지 않음 |
| 다종교 RAG | 전통별 승인 자료·검색·인용·경계 격리·오라우팅 검증 | 해당 전통의 전문가·사용자 검증 |
| 7종교 플랫폼 | 7개 전통별 선택·전환·대화·출처·저장 흐름과 서비스 범위 검증 | 7개 각각의 대상 사용자 근거, 누락 전통 명시 |
| Marketing Agent 자동화 | 정의된 채널·실행 범위에서 실제 실행·중복 방지·성과 귀속·중지 확인 | 운영자 수용과 실제 반응·성과. 게시/발신은 세션의 사용자 권한 범위에 따름 |
| App Ops 자동화 | 실제 모니터링·장애 탐지·알림·복구/롤백 시나리오와 감사 기록 | 운영자의 장애 대응 과제 수행 결과 |
| GitHub Dev/QA 자동화 | 실제 PR/브랜치의 지정 테스트·빌드·실패 차단·산출물 확인 | 개발자/검토자가 결과를 사용하고 실패 원인을 추적할 수 있음 |
| Chief Agent 자동화 | 하위 작업 배정·상태·증거 통합, 실패 전파·중지·재시도·권한 경계 확인 | 운영자가 계획과 실행 결과를 이해하고 수정·중지 가능 |

## 기능별 보고 양식

### Safety 필수 체크리스트

#### 위기 응답 원칙

위험 입력의 요청별 불변조건은 `Psychology model call = 0`, `Religion model call = 0`, `OpenAI client creation = 0`이다. 최초 요청(cold)과 일반 대화 후 클라이언트가 이미 존재하는 상태(warm)를 모두 검증한다. warm에서는 요청 이전의 생성 횟수가 아니라 해당 위험 요청의 증가량이 0이어야 한다. 기존 직접 OpenAI 어댑터를 외부에서 미리 구성한 경우에도 위기 처리 중 추가 생성·모델 호출이 없어야 한다. 운영에서는 실제 telemetry로 별도 확인한다.

- 위기 신호가 감지되면 일반 종교 상담·말씀 추천·전문 에이전트 생성을 중단한다.
- 고정 안내에는 설교·훈계·비난·죄책감 유발·신앙 강요를 넣지 않는다.
- ‘지금 안전한 곳에 계신가요?’처럼 현재 안전을 확인하는 질문을 표시한다. 질문 표시만으로 실제 안전이 확인됐다고 기록하지 않는다.
- 신뢰할 수 있는 주변 사람의 동행과 상담 전문가·의료진의 도움을 권한다.
- 급박한 위험은 현재 지역의 응급 서비스로 안내한다. 현 구현의 전화 버튼은 한국 109·112·119이며 한국 기준임을 명시한다. 언어만으로 사용자의 실제 위치를 단정하지 않는다.
- 응급 전화는 사용자가 선택하는 연결 기능이며 자동 발신·자동 구조 요청·위치 전달을 했다고 표현하지 않는다.

연락처 확인 근거(2026-09-10): [보건복지상담센터 109 안내](https://www.129.go.kr/109), [정부 112·119 긴급신고 안내](https://www.korea.kr/news/policyNewsView.do?newsId=148915282).

#### Safety 등급

등급 명칭은 사용자가 지정한 `none / low / medium / high / critical`로 통일한다. 아래 의미와 기존 값 연결은 구현 전 정책 초안이며 임상적 진단 척도가 아니다.

| 등급 | 제안 의미 | 기존 riskLevel과 호환 |
| --- | --- | --- |
| `none` | 현재 규칙에서 위험 신호를 감지하지 못함. 안전 보증은 아님 | 0 |
| `low` | 명확한 위기 신호 없이 추가 맥락 확인이 필요한 상태 | 현재 독립 판정 없음. 세부 기준·테스트 확정 전 자동 부여하지 않음 |
| `medium` | 현재 level 1의 간접 위험 신호 등으로 지원 경로가 필요한 상태 | 1; 기존 일반 생성 차단 유지 |
| `high` | 현재 level 2의 직접적 자해/자살·타해 의도 신호 | 2; 일반 종교/모델 응답 차단 유지 |
| `critical` | 현재 level 3의 임박성 또는 응급 신호 | 3; 즉시 고정 위기 안내 유지 |

현재 API는 정수 0~3을 사용한다. 이 명칭 정의만으로 API를 문자열이나 0~4로 변경하지 않는다. `low`를 구현할 때는 `none`과 구분되는 명시적 기준·양쪽 공통 예제·호환 필드를 함께 설계한다. 단순 슬픔·불안·죄책감만으로 자동 위험 등급을 올리거나, 농담·인용·가사라는 이유로 기존 위험을 낮추지 않는다. 탐지 확신도와 위험 등급은 별개다.

표현 범위에는 사용자가 지정한 12범주를 포함한다. [범주별 예제·기대 라우팅·문맥 한계](docs/guides/SAFETY_CATEGORIES.md)를 기준으로 Flutter/backend 공통 사례를 실행한다. 범주 이름만으로 모든 문장을 같은 위험 단계로 판정하지 않는다.

추가된 7개 조건(검증 직후 실행, 모든 전문/모델 호출 이전 실행, high/critical 일반 응답 차단, 위기 fallback 보존, 비밀/오류/모델 원문 비노출, Flutter/backend 판정 일치, 실제 서버 동일 동작)을 모두 판정 대상으로 삼는다. 항목별 코드·테스트와 운영에서 남은 검증은 [Safety 검증 보고서](docs/archive/SAFETY_VERIFICATION_REPORT.md)의 7조건 표에 기록한다. 아래 기존 다섯 조건도 유지한다.

사용자가 지정한 다음 다섯 조건을 모두 확인한다. TEST 근거와 실제 서버 REAL 근거를 별도로 기록한다.

- [ ] 기존 safety test 통과: 앱 위기 감지·상태 전이와 서버의 기존 위기 회귀 테스트 결과를 대상 버전에 연결한다.
- [ ] 고위험/자해/자살 표현 테스트 통과: 테스트한 표현·위험 단계·기대 지원 경로를 명시한다. 제한된 예제 통과를 모든 표현의 탐지 보장으로 해석하지 않는다.
- [ ] OpenAI 호출 전에 safety가 실행됨: 코드 순서뿐 아니라 위험 입력의 제공자 호출 횟수가 0임을 검증한다. 앱 공통 인증·요청 형식 검증은 safety 이전에 실행될 수 있다.
- [ ] 실제 서버 환경 검증: 대상 서버의 배포 버전·설정·요청 결과와 위기 응답, downstream 호출 없음의 관측 근거를 기록한다. localhost/mock 결과로 대체하지 않는다.
- [ ] fallback 검증: 모델 오류·timeout·초기화/잘못된 출력에서 일반 대화의 대체 응답을 검증하고, 위기 입력은 일반 fallback으로 내려가지 않고 위기 응답을 유지하는지 확인한다. 실제 서버의 장애 시나리오 근거도 별도로 남긴다.

위 체크박스는 완료 판정용 빈 양식이다. 현재 충족 여부는 [STATUS의 Safety 상세](ONARIA_STATUS.md#safety--위기대응-상세)에 기록한다. 실제 서버에 미치는 영향이 있는 장애 주입은 격리된 검증 환경과 허용된 작업 범위 안에서 수행한다.

```text
기능명:
우선순위:
CODE:
TEST:
REAL:
USER:
근거 파일/테스트:
남은 리스크:
다음 행동:
```

CODE·TEST·REAL·USER는 각각 상태와 확인 범위를 적는다. 상태값은 공통 단계에서 정의한 값을 사용하고, mock·skip·제외 범위를 해당 단계에 명시한다. 우선순위는 ROADMAP의 단계와 이유를 적으며 제안인지 확정인지 구분한다. 우선순위가 높아도 검증 완료를 의미하지 않는다.

근거 파일/테스트에는 링크와 함께 대상 버전, 검증 일시·환경, 실행 방법, 기대/실제 결과를 기록하거나 해당 내용을 담은 보고서를 연결한다. USER 근거에는 비식별 사용자 역할·과제·결과를 포함한다. 남은 리스크는 확인되지 않은 조건과 영향, 다음 행동은 이를 해결하거나 검증할 구체적인 작업을 적는다.

API 키·비밀번호·원문 상담 내용·개인정보는 증거 문서에 붙이지 않는다. 사용자 실적 원본이 외부에 있으면 문서에는 접근 가능한 비식별 결과 요약과 보관 위치만 남긴다.

## 작업 전체 보고 양식

기능별 9개 항목 보고와 별도로, 작업 묶음의 진행 상황과 종료 보고에는 아래 양식을 사용한다.

```text
현재 Gate:
완료된 항목:
미완료 항목:
가장 큰 리스크:
다음 3개 작업:
생성/수정한 파일:
기존 테스트 영향:
```

현재 Gate에는 대상 범위와 CODE·TEST·REAL·USER 중 검증을 진행하거나 기다리는 단계를 명시한다. 여러 기능의 단계가 다르면 하나의 완료 상태로 합치지 않는다. 완료된 항목과 미완료 항목은 근거 범위에 맞게 적는다. 가장 큰 리스크는 다음 검증을 가로막거나 결과를 바꿀 수 있는 핵심 불확실성을 적는다.

다음 3개 작업은 의존 순서대로 번호를 붙인다. 생성/수정한 파일은 이번 작업에서 실제로 변경한 파일을 연결한다. 기존 테스트 영향에는 코드 변경 여부, 실행 여부·결과·skip/mock 범위, 아직 확인하지 못한 영향을 구분한다. 과거 통과 결과를 이번 실행으로 표현하지 않는다.

## 판정 예시

- onaria APK 설치·실행 성공: 설치/시작 범위의 REAL 근거이며 7화면 전체나 OpenAI 연결 성공은 아니다.
- backend 315개 통과: 실행 당시 테스트 범위의 TEST 근거이며 실제 OpenAI·운영 RAG·사용자 수용을 뜻하지 않는다.
- 임시 SQLite 재시작 검증: 비용 원장의 로컬 영속성 근거이며 Persistent Memory나 제공자 청구 검증을 대신하지 않는다.
- 자료 후보 등록·사용권 안내 확보: 검토 준비 근거이며 승인 corpus·전체 서비스 사용권 완료를 뜻하지 않는다.
- USER 완료는 실제 검증 결과를 확보한 뒤에만 기록한다. 단순 요청·침묵·설치 허용을 사용자 수용 결과로 해석하지 않는다.
