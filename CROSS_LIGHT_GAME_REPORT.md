# 십자가 빛 모으기 — 2026-09-11

현재 Gate: 십자가 미니게임 CODE·TEST. 아리와 나눈 별도 기획 원문은 프로젝트에서 확인되지 않아 기존 Cross Light 규칙을 기준으로 개발했다. 사용자에게 규칙 확인을 요청했으며, 응답이 없는 동안 기존 방식을 유지했다.

완료된 항목:
- 메인 메뉴의 `십자가 미니게임`, 말씀과 작은 기록의 `십자가 빛 모으기`에서 진입한다.
- 시작 후 5초씩 쉬어 가며 평안·희망·사랑·은혜·용기·용서 중 아직 고르지 않은 단어를 선택한다. 순서는 자유이며 여섯 단어를 모으면 완료한다.
- 십자가를 여섯 조각으로 표시하고 선택한 단어의 조각을 켠다. 선택한 단어와 쉬어 가는 안내를 표시한다. 후속 사용자 기준에 따라 숫자 카운트다운과 숫자 수집 표시는 제거했다.
- 잠시 쉬기·이어서 하기, 앱이 백그라운드로 이동할 때 일시정지, 다른 화면에 가려졌을 때 일시정지를 지원한다. 복귀 후 직접 이어서 시작한다.
- 진행 중 처음부터 다시 하기는 확인창을 표시한다. 취소하면 모은 조각을 유지한 채 일시정지 상태로 남는다. 완료 후에는 한 번 더 플레이할 수 있다.
- 완료·재시작 시 십자가를 볼 수 있도록 화면 위로 이동한다. 기기의 움직임 줄이기 설정에서는 조각 전환과 스크롤 애니메이션을 사용하지 않는다.
- 게임 완료 횟수는 판마다 한 번 기록한다. 재시작·일시정지·중단을 완료로 세지 않는다. 기존 작은 발자취의 고요한 순간 성취와 연결된다.

미완료 항목: 실제 Android/iOS 기기 조작·화면 검증, 아리와 논의한 원래 규칙과의 일치 확인, 사용자 플레이 피드백. 진행 중인 판은 화면 수명 동안만 유지되며 화면을 닫거나 앱을 종료하면 새 판으로 시작한다. 완료 성취·횟수는 기존 기기 저장을 사용한다.

가장 큰 리스크: 원래 대화의 규칙이 다른 경우 요구사항 조정이 필요하다. 자동 테스트는 실제 단말 조작감을 대신하지 않는다.

다음 3개 작업:
1. 생성한 APK에서 시작 → 여섯 단어 선택 → 완료 → 다시하기를 실제 기기로 확인한다.
2. 홈 이동·복귀·잠시 쉬기·재시작 취소와 큰 글자 설정을 확인한다.
3. 원래 합의한 게임 규칙과 사용자 피드백을 반영한다.

생성/수정한 파일: [게임 규칙](lib/engagement/mini_games/cross_light/cross_light_game.dart), [게임 화면](lib/engagement/mini_games/cross_light/cross_light_page.dart), [추가 진입점](lib/engagement/engagement_page.dart), [규칙 테스트](test/cross_light_game_test.dart), [화면 테스트](test/cross_light_page_test.dart), [현재 상태](ONARIA_STATUS.md), [기능 코드 연결표](PROCESS_CODE_MAP.md), 본 보고서.

기존 테스트 영향: 신규 규칙 테스트 2개·화면 테스트 3개를 추가했다. 전체 `flutter test --no-pub`는 **65개 통과·기존 skip 1개**, 실패 0. 정적 분석 error/warning 0·기존 info 6개. 최종 `flutter build apk --debug --no-pub` 성공. [테스트 APK](build/app/outputs/flutter-apk/app-debug.apk)를 생성했다. API 및 backend 제품 코드는 변경하지 않았으며 실제 AI API를 호출하지 않았다. 실제 단말 설치·실행은 하지 않았다.

검증 환경: Windows, Flutter 3.47.2. 자동 테스트는 5초 간격·중복 선택·6조각 완료·중지 중 시간 보존·재시작·백그라운드 복귀·판별 완료 횟수·320×640 화면/글자 2배·움직임 줄이기를 포함한다.

## 2026-09-11 USB 설치 후속 확인

- 연결 기기: Samsung SM-S908N. USB 디버깅 승인 후 설치했다.
- debug APK는 기존 앱과 서명이 달라 업데이트가 거부됐다. 기존 앱을 삭제하지 않고 프로젝트의 릴리스 서명으로 최신 코드를 다시 빌드했다.
- `flutter build apk --release --no-pub` 성공. `app-release.apk`를 `adb install -r`로 설치하여 `Success` 확인.
- 설치 버전 `0.4.0+5`, 기기 `lastUpdateTime=2026-09-11 04:06:35`. `am start -W` 결과 `Status: ok`로 앱 실행 확인.
- REAL은 설치·시작 범위만 확인했다. 실제 게임 한 판 완료·알림·공유 검증이나 사용자 수용을 뜻하지 않는다. API 호출은 하지 않았다.

## 사용자 경험 기준 반영

사용자가 지정한 기준: 랭킹·경쟁·높은 점수·실패/패배·복잡한 조작 제외. 20~60초, 간단한 터치, 마음 안정, 말씀 연결, 작은 성취감, 다시 하고 싶은 경험을 지향한다.

- 기본 흐름은 5초씩 쉬고 단어를 한 번 누르는 동작 여섯 번으로 약 30초다. 사용자가 더 천천히 하거나 쉬어도 실패·시간 초과로 처리하지 않는다.
- 단어를 한글로 간결하게 표시하고 일반 화면에서는 두 열로 배치했다. 큰 글자 설정에서는 한 열로 표시해 터치 영역과 가독성을 확보한다.
- 숫자 카운트다운·숫자 수집 표시를 부드러운 안내로 바꾸고 십자가 조각이 켜지는 모습으로 진행을 표현한다. 점수·순위·승패·보상 경쟁을 추가하지 않는다.
- 완료 화면은 로컬 번들의 `MAT_11_28` 본문과 마태복음 11:28 출처를 표시한다. 자료를 불러오지 못한 경우 기존 창작 묵상 문구로 마무리하고 말씀이라고 표시하지 않는다. AI API 요청은 없다.
- 완료 시 작은 쉼을 인정하는 문구와 자유로운 돌아가기·다시하기를 제공한다. 안정감이나 재방문 의향의 실제 효과는 사용자 검증이 필요하다.
- 실제 번들 말씀과 출처 표시 테스트를 추가했다. 전체 Flutter **66개 통과·기존 skip 1개**, 정적 분석 error/warning 0·기존 info 6개. 릴리스 APK 빌드 성공.
- SM-S908N에 `adb install -r` 업데이트 `Success`, 앱 실행 `Status: ok` 확인. 기기 `lastUpdateTime=2026-09-11 04:13:38`. 기존 앱을 삭제하지 않았다. 실제 플레이의 안정감·재사용 의향은 아직 검증하지 않았다.

## 점·별빛 스케치 반영

사용자가 보낸 `· / ✧ / ✦ → 십자가` 스케치를 흩어진 별빛이 십자가로 모이는 시각 흐름으로 해석했다. 빛 직접 터치/자동 수집 중 선호를 확인 요청했으며, 별도 답변이 없는 동안 앞서 지정한 간단한 터치 기준을 유지했다.

- 배경의 작은 점과 여섯 개의 흩어진 `✧`를 표시한다. 별빛을 직접 터치하면 `✦`로 바뀌며 900ms 동안 십자가의 정해진 위치로 이동한다.
- 마지막 빛을 모으면 가느다란 가로·세로 선이 부드럽게 나타난다. 사각 타일과 단어 선택 버튼은 제거했다.
- 실제 터치 영역은 48×48 논리 픽셀이며, 화면 읽기에는 각 별빛의 이름·선택 상태와 터치 동작을 제공한다. 움직임 줄이기 설정에서는 위치 이동과 선 전환을 즉시 적용한다.
- 5초 간격의 여섯 번 터치, 약 30초 기본 흐름, 일시정지, 재시작, 마태복음 11:28 연결을 유지한다. 실제 AI API는 사용하지 않는다.
- [별빛 화면](lib/engagement/mini_games/cross_light/cross_light_sky.dart)과 [터치·이동 테스트](test/cross_light_sky_test.dart)를 추가했다. 기존 화면 테스트는 실제 십자가의 가로·세로 정렬을 검증하도록 갱신했다.
- 최신 검증: Flutter 전체 67개 통과·기존 skip 1개. 정적 분석 error/warning 0·기존 info 6개. 릴리스 APK 빌드 성공.
- SM-S908N USB 업데이트 `adb install -r` 결과 `Success`, 앱 실행 요청 `Status: ok`. 기기 잠금 화면이 표시되어 실제 게임 화면의 글리프·배치와 플레이는 확인하지 못했다. 기존 앱 데이터는 삭제하지 않았다.

## 마음 돌봄 순서 연결

- 감정 선택 → 감정 강도 → 기존 AI 마음대화 → 말씀 → 작은 행동 → 십자가 미니게임 → 오늘의 마음카드 → 저장 / 공유 순서로 연결했다. API 구현은 수정하지 않았다.
- 행동 선택 시 게임을 열고, 완료 후 `오늘의 마음카드 보기`를 누르면 선택한 감정·강도·말씀·행동을 유지한 마음카드를 표시한다. 게임에서 뒤로 돌아오면 행동 선택 화면으로 돌아온다. 메뉴에서 단독 실행한 게임은 기존 돌아가기 동작을 유지한다.
- 마음카드에 공유 미리보기 진입을 추가했다. 공유 이미지에는 기존 공개용 카드 정책을 적용하며 대화 내용·감정 강도·개인 요약을 담지 않는다. 말씀을 건너뛴 흐름에서도 카드 저장이 가능하다.
- 정상 응답 및 모의 API 실패 경로 모두에서 말씀·행동·게임 진입·취소·완료 결과·공유 미리보기·카드 저장을 검증했다. 게임 자체 테스트는 여섯 빛 수집 후 완료 결과 반환을 검증한다. 실제 AI 호출 없이 전체 Flutter 67개 통과·기존 skip 1개, 정적 분석 error/warning 0·기존 info 6개.
- 릴리스 APK 빌드 성공 및 SM-S908N에 기존 데이터를 유지하는 USB 업데이트 `Success`, 앱 실행 요청 `Status: ok`. 단말에서 실제 대화부터 공유까지 전체 흐름을 플레이한 검증은 수행하지 않았다.

## 처음의 흔들림 → 완료의 잔잔한 빛

- 흩어진 별을 `✦`·`✧`로 섞어 표시하고 처음 6초 동안 가로 최대 3px·세로 최대 2px로 천천히 흔들리다 가라앉게 했다. 터치 영역은 48px로 고정한다.
- 수집한 별의 흔들림은 즉시 멈춘다. 십자가 완성 시 배경의 옅은 빛과 선 주변의 부드러운 빛 번짐이 나타나며 이후 깜빡임 없이 유지된다. 다시 시작하면 첫 흔들림을 재생한다.
- 움직임 줄이기 설정에서는 흔들림과 전환 애니메이션을 생략한다. 별 이동·정렬, 완료 정지, 고정 터치 영역 및 해당 설정을 포함해 Flutter 전체 68개 통과·기존 skip 1개. 정적 분석 error/warning 0·기존 info 6개.
- 릴리스 APK 빌드 성공, SM-S908N USB 업데이트 `Success`, 앱 실행 요청 `Status: ok`. 실제 단말에서 애니메이션을 육안으로 확인한 것은 아니다. AI API 호출 없이 검증했다.
