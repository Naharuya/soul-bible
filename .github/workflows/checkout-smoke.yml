name: Checkout Action Smoke

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "checkout action succeeded"
